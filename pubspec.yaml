name: flutter_body
description: RehabAssist - AI 復健訓練助理
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  camera: ^0.11.2+1          # ✅ 新增
  onnxruntime_v2: ^1.23.2    # ✅ 新增
  flutter_tts: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:                    # ✅ 移到這裡（縮排在 flutter: 底下）
    - assets/rtmdet.onnx
    - assets/rtmpose_wholebody.onnx